# 値幅スキャン・材料レポート・スクリーニングマスタ・ダッシュボード共通
# （この 1 ファイルでインストール。別ファイルの requirements-movers は廃止）
#
# 注意: pip で入れる名前は jquants-api-client（ハイフン付き）。
# コード側は import jquantsapi のまま（公式クライアントの仕様）。
# 2.x は Python 3.10 以上。3.9 以下なら 1.9 系が入る（>=1.9.0 で両対応）。

jquants-api-client>=1.9.0
# make_screening_master_v2.py --yfinance 用 / 開示が薄い銘柄の Yahoo 年次損益フォールバック（任意）
yfinance>=0.2.36
pandas>=2.0.0
# pd.read_html（カブラボ/株探の夜間PTS表解析・make_pts_mover_report.py）+ BeautifulSoup
lxml>=4.9.0
beautifulsoup4>=4.12.0
pyarrow>=14.0.0
openpyxl>=3.1.0
requests>=2.31.0
python-dotenv>=1.0.0
pdfminer.six>=20221105
# 営業日ベースの陳腐化判定（市況スナップショット・セクター鮮度ガード / lib/snapshot_utils.py）。
# 未導入でも weekday のみで動くが、祝日を除外できず連休明けに誤検知し得るため必須扱い。
jpholiday>=0.1.0

# ダッシュボード（Streamlit）
streamlit>=1.33.0
feedparser>=6.0.0
pyyaml>=6.0.0

# JPEG レポート画像化（send_report_jpeg_discord.py + lib/md_to_jpeg.py）
markdown>=3.5
playwright>=1.40.0
# PM 2026-05-23 確定: Discord 8MB 上限超過時の自動再圧縮で使用（ensure_under_discord_limit）
Pillow>=10.0.0
